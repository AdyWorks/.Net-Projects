<?xml version="1.0" encoding="utf-8" ?>
<configuration>
    <startup> 
        <this should result in a conflict the local version>
        <this will be a change that hmm to be reverted />
        <and here_on_the_server be another change>
    </startup>
</configuration>
